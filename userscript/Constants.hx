package userscript;

class Constants
{
    public static var USERSCRIPT_VARIABLES = [
        "name", "namespace", "version", "description", "downloadURL", "exclude",
        "icon", "include", "match", "require", "resource", "run-at", "unwrap",
        "updateURL"
    ];

    public static var CODE_PLACEHOLDER = "####!\\CODE_HERE!\\####";
}
